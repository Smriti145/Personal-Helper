# React Native CLI

This app uses React Native Community CLI 20.1.0 and React Native 0.86.3. Preserve the native Android/iOS projects. Do not reintroduce Expo, Expo Go, browser UI or website hosting.

Use the shared routine domain in ../shared. Keep medication instructions user-controlled. Authentication tokens belong in native secure storage. Never delete server/.wrangler/state or server/.dev.vars during cleanup.

Validate changes with npm run typecheck and npm run bundle. Native binary compilation requires the platform SDKs.
